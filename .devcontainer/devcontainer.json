{
    "name": "pokedex",
    "build": {
        "dockerfile": "../tools/docker/Dockerfile",
        "context": "../tools/docker/"
    },

    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": [
                "rust-lang.rust-analyzer",
                "yzhang.markdown-all-in-one"
            ]
        }
    },

    "forwardPorts": [8000],

    // "mounts": [
    //     "source=${localWorkspaceFolder}/pokedex.db,target=/workspaces/pokedex,type=bind,consistency=cached"
    // ],

    // "postCreateCommand": "sqlite /tools/sqlite/pokedex.sqlite"
}